# mntpack 0.6.7 (2026-03-18)

## Fixed
- Managed `mntpack` installs that still record the legacy owner `MINTILER-DEV/mntpack` can now self-update correctly.
- Reserved-name and protection checks for the managed `mntpack` package now accept both the canonical `mntpack/mntpack` owner and the legacy owner during migration.
